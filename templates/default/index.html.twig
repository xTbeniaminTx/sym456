{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% set string= "some string \n from \n database" %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .notice {
            background: #718C00;
        }

        .warning {
            background: #AA3333;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>



        {{ path('home') }} <br>
        {{ url('home') }} <br>
        {{ asset('images/logo.png') }} <br>
        {{ absolute_url(asset('images/logo.png')) }} <br>

        <img src="{{ asset('images/logo.png') }}" alt="logo example test">

        <p>Glogal traking code is: {{ ga_code }}</p>

        {{ app.user }}

        {{ app.request.get('param') }}

        {{ app.environment }}

        {{ dump(app.session) }}

        {{ render(controller(
            'App\\Controller\\DefaultController::mostPopularPosts',
            {'number': 2}
        )) }}

    </div>
{% endblock %}

{% block javascripts %}

{% endblock %}

